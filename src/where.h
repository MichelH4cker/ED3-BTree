#ifndef __WHERE_H__
#define __WHERE_H__

#include <string.h>

#include "main.h"
#include "funcoesFornecidas.h"
#include "index_file.h"

/**
 * @brief faz o comando "where"
 * @param fp arquivo em que ocorrerá a busca
*/
void where(FILE *fp);

#endif
#include "where.h"

void where(FILE *fp){
    int n;
    scanf("%d", &n);

    
    header_bin header;
    header = readHeaderBin(fp);
    register_bin reg;

    int target;

    for (int i = 0; i < n; i++) {
        
    }
    search(fp);

    
}